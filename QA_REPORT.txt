VELNOX V1.5.1 EXIT PATCH QA REPORT
Generated: 2026-08-19

PATCH SCOPE
This QA report covers only the V1.5.1 Exit control patch prepared from current GitHub main.
The V1.5 measurement engine, benchmark generator, styles.css, app.js, manifest, icons and benchmark semantics are unchanged by this patch.

PATCH-SPECIFIC STATIC QA
PASS  sw.js JavaScript syntax (node --check)
PASS  Exit inline JavaScript syntax (node --check after extraction from index.html)
PASS  No duplicate HTML element IDs (82 IDs checked)
PASS  exitBtn exists exactly once
PASS  Every getElementById reference used by the Exit patch resolves to an element in index.html
PASS  Exit control reuses the existing icon-button visual system; no CSS file change required
PASS  Exit control remains hidden in an ordinary browser tab unless browser fullscreen is active
PASS  Exit control is available in standalone/installed PWA display mode
PASS  Exit behavior requests document.exitFullscreen() before attempting window.close()
PASS  Browser-blocked close path displays an explicit fallback instead of claiming success
PASS  Exit fallback message is implemented in both Thai and English
PASS  About text updated to v1.5.1
PASS  Service Worker app cache bumped to velnox-app-v1.5.1
PASS  Benchmark cache name and benchmark network-first/no-store implementation are unchanged
PASS  No Cloudflare token or secret is added by this patch
PASS  Country / Worldwide benchmark files are not modified by this patch

INHERITED V1.5 BASELINE
The existing V1.5 static/build QA, accuracy-first measurement behavior, measurement confidence,
verified benchmark architecture, no-fabrication rule, privacy contract, TH/EN interface,
and result-page UX remain inherited from the current V1.5 main baseline.

RUNTIME QA STILL REQUIRED AFTER DEPLOYMENT
- Installed PWA: Exit button is visible on Home
- Browser tab: Exit button is hidden before fullscreen
- Fullscreen browser test: Exit button appears while fullscreen is active
- Press Exit in fullscreen: fullscreen exits first
- On a platform that allows script close: PWA/window closes
- On a platform that blocks script close: bilingual fallback is shown and app remains truthful
- TH/EN switching updates Exit tooltip/fallback language
- Existing Home, History, Settings, Test, Result and Compare flows remain unaffected
- Service worker updates from velnox-app-v1.5.0 to velnox-app-v1.5.1 correctly

IMPORTANT DEPLOYMENT NOTE
At the final repository check made while preparing this patch, benchmarks/index.json on main was still ready:false
and no automated benchmark refresh commit had appeared yet. Do not upload this patch to main while the first ALL-country
benchmark workflow is still running, because changing main can make the workflow's final git push fail as non-fast-forward.
Upload the patch only after the benchmark workflow has completed (success or failure) and the current main state has been rechecked.

NO 10/10 CLAIM
This patch has passed patch-specific static QA only. It has not yet been runtime-tested on the deployed GitHub Pages PWA.
